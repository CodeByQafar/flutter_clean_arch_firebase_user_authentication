import 'package:flutter/material.dart';

class BorderRadiuses {
  // AuthScreen border radiuses
  static final BorderRadius signUpButtonRadius = BorderRadius.only(
    topLeft: Radius.circular(33),
  );
}
